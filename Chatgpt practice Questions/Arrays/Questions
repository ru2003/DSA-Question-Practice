1.Find the largest element in an array.

2.Find the second largest element in an array.

3.Find the missing number in an array containing numbers from 1 to n (one missing).

4.Find duplicates in an array.

5.Rotate an array to the right by k steps.

6.Find the first non-repeating element in an array.

7.Move all zeros to the end of array without changing order of non-zero elements.

8.Merge two sorted arrays into one sorted array.

9.Check if an array is sorted (ascending order).

10.Count the number of occurrences of a specific element in an array.

